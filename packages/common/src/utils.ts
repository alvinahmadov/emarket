import CommonUtils   from './utilities/common';
import FakeDataUtils from './utilities/fake-data';
import GeoUtils      from './utilities/geolocation';

export const getRandomInt = FakeDataUtils.getRandomInt;
export const getDistance = GeoUtils.getDistance;
export const toDate = CommonUtils.toDate;
